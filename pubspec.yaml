name: flutter_tests
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dartx: ^0.7.1
  rxdart: ^0.27.1
  bloc_test: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test:
  mockito: ^5.0.10

flutter:
  uses-material-design: true
